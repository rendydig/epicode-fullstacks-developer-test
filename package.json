{
  "name": "epicode-enrollment-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^3.1.0",
    "turbo": "^1.11.2"
  },
  "packageManager": "bun@1.0.20"
}
